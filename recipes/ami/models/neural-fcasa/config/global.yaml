# global constants that shared across config groups

### PARAMS
sr: 16000
n_fft: 512
hop_length: 160

n_mic: 8
n_src: 6
dim_latent: 64

autocast: True
working_directory: .
primary_subdir: dist=${model.distribution}-param=${model.dist_param}
output_dir: ${hydra:runtime.output_dir}

dataset_name: chunk.derev-hop${hop_length}
data_dir: ${working_directory}/../../